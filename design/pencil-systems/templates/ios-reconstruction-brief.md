# Pencil iOS reconstruction brief

Read `design/pencil-systems/DESIGN.md` and import `design/pencil-systems/core-ios.lib.pen` before editing.

Recreate the screen from `[SIMULATOR_SCREENSHOT]` using `[SOURCE_VIEW]` as the structural reference. Preserve the `[WIDTH]×[HEIGHT]` viewport, safe areas, authentic content, user goal, and target step. Use editable Pencil layers, library instances, variables, slots, and Lucide/iconoir icons. Keep the screenshot locked behind the reconstruction.

Audit idle, loading, empty, success, error, recovery, focus, reduced motion, dark mode, 44pt targets, and WCAG AA contrast. List every deviation and its reason. Export a comparison image before handoff.
